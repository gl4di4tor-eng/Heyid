/**
 * SOCIAL — external profile links. Empty/null until real accounts exist;
 * components should hide a link whose value is null rather than render
 * a dead href.
 */
export const SOCIAL = {
  instagram: null as string | null,
  tiktok: null as string | null,
  x: null as string | null,
  discord: null as string | null,
};
